# Installation

Use the interactive installer:

```bash
bash install.sh
```

It prompts for all required values and tells you where to get Telegram API credentials.

After setup:

```bash
source .venv/bin/activate
python driver_mtproto.py
```
